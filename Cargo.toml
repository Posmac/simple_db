[package]
name = "simple_db"
version = "0.1.0"
edition = "2024"

[features]
client = []
server = []

[dependencies]
socket2 = "0.6.3"
nix = { version = "0.31.2", features = ["event", "poll"] }
libc = "0.2.183"
